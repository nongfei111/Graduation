# CLAUDE 智能门铃系统 - 服务器连接与远程控制指南

## 一、服务器信息

### 1.1 阿里云服务器配置

| 项目 | 值 |
|------|-----|
| 实例 ID | 7a518455b93b47cca76ffbe076ae2112 |
| 实例名称 | Alibaba Cloud Linux-vmgc |
| 公网 IP | 8.134.196.56 |
| 私有 IP | 172.19.47.170 |
| 地域 | 华南 3（广州） |
| 镜像 | Alibaba Cloud Linux 3.21.04 |
| 运行状态 | 运行中 |
| 到期时间 | 2026 年 10 月 11 日 00:00:00 |

### 1.2 服务端口

| 端口 | 用途 | 说明 |
|------|------|------|
| 5000 | Flask API | 提供 HTTP 接口 |
| 1883 | MQTT Broker | 消息队列服务（公共 Broker） |

> ⚠️ **注意**: 需要在阿里云控制台开放端口 5000 和 1883

---

## 二、系统架构

```
┌─────────────────┐         MQTT          ┌─────────────────┐
│   手机 APP/Web  │ ────────────────────> │  阿里云服务器   │
│   (远程控制)    │                       │  (8.134.196.56) │
└─────────────────┘                       └─────────────────┘
         │                                        │
         │ HTTP/HTTPS                             │ MQTT
         │                                        │
         ▼                                        ▼
┌─────────────────┐                       ┌─────────────────┐
│   Firebase      │                       │  MQTT Broker    │
│   (推送通知)     │                       │ (broker.emqx.io)│
└─────────────────┘                       └─────────────────┘
                                                 │
                                                 │ MQTT
                                                 │
                                                 ▼
                                        ┌─────────────────┐
                                        │   树莓派设备     │
                                        │  (门铃主机)      │
                                        └─────────────────┘
                                                 │
                                                 │ GPIO
                                                 ▼
                                        ┌─────────────────┐
                                        │  步进电机 + 锁具  │
                                        └─────────────────┘
```

---

## 三、MQTT 通信配置

### 3.1 MQTT 主题定义

| 主题 | 方向 | 用途 |
|------|------|------|
| `doorbell/unlock` | 服务器→设备 | 开锁指令 |
| `doorbell/status` | 设备→服务器 | 设备状态 |
| `doorbell/visitor` | 设备→服务器 | 访客通知 |
| `doorbell/alert` | 设备→服务器 | 报警通知 |

### 3.2 消息格式

#### 开锁指令（服务器→设备）
```json
{
    "action": "unlock",
    "user_id": "user123",
    "method": "app",
    "timestamp": "2026-04-10T12:30:00"
}
```

#### 设备状态（设备→服务器）
```json
{
    "device_id": "raspi_doorbell_001",
    "status": "online",
    "timestamp": "2026-04-10T12:30:00",
    "unlock_count": 15
}
```

#### 开锁结果（设备→服务器）
```json
{
    "device_id": "raspi_doorbell_001",
    "type": "unlock_result",
    "success": true,
    "user_id": "user123",
    "method": "app",
    "timestamp": "2026-04-10T12:30:01"
}
```

---

## 四、API 接口文档

### 4.1 远程开锁接口

**URL**: `POST http://8.134.196.56:5000/api/unlock`

**请求体**:
```json
{
    "user_id": "user123",
    "method": "app"
}
```

**响应**:
```json
{
    "success": true,
    "data": {
        "command_id": "20260410123000",
        "status": "sent",
        "message": "开锁指令已下发，等待设备执行"
    }
}
```

### 4.2 设备状态查询

**URL**: `GET http://8.134.196.56:5000/api/status?device_id=raspi_doorbell_001`

**响应**:
```json
{
    "success": true,
    "data": {
        "device_id": "raspi_doorbell_001",
        "status": "online",
        "last_seen": "2026-04-10T12:30:00"
    }
}
```

### 4.3 服务器信息

**URL**: `GET http://8.134.196.56:5000/api/info`

**响应**:
```json
{
    "success": true,
    "data": {
        "instance_id": "7a518455b93b47cca76ffbe076ae2112",
        "instance_name": "Alibaba Cloud Linux-vmgc",
        "region": "华南 3（广州）",
        "public_ip": "8.134.196.56",
        "private_ip": "172.19.47.170"
    }
}
```

### 4.4 健康检查

**URL**: `GET http://8.134.196.56:5000/api/health`

**响应**:
```json
{
    "status": "ok",
    "timestamp": "2026-04-10T12:30:00",
    "mqtt_connected": true
}
```

---

## 五、树莓派端配置

### 5.1 环境要求

- 树莓派 4B/5 (推荐 4GB+ 内存)
- Raspberry Pi OS (64 位)
- Python 3.9+
- 摄像头模块
- 步进电机 (28BYJ-48) + ULN2003 驱动板
- 蜂鸣器 (GPIO 17)
- 触摸屏 (可选)

### 5.2 安装依赖

```bash
# 系统依赖
sudo apt update
sudo apt install -y python3-pip python3-venv libatlas-base-dev

# 创建虚拟环境
cd /path/to/graduation
python3 -m venv venv
source venv/bin/activate

# Python 依赖
pip install --upgrade pip
pip install paho-mqtt requests numpy opencv-python
```

### 5.3 配置文件

编辑 `config/settings.json`:

```json
{
  "system": {
    "device_id": "raspi_doorbell_001"
  },
  "network": {
    "server_host": "8.134.196.56",
    "server_port": 5000,
    "mqtt_broker": "broker.emqx.io",
    "mqtt_port": 1883
  },
  "motor": {
    "type": "28BYJ-48",
    "pins": [1, 7, 8, 25],
    "steps_per_rev": 2048,
    "unlock_angle": 90
  }
}
```

### 5.4 启动远程通信服务

```bash
# 测试模式 (模拟硬件)
python3 remote_comm.py --simulator

# 正常模式 (真实硬件)
python3 remote_comm.py

# 仅测试开锁功能
python3 remote_comm.py --test
```

---

## 六、测试步骤

### 6.1 本地测试

```bash
# 1. 测试步进电机
python3 tests/test_stepper_motor.py

# 2. 测试 MQTT 连接
python3 src/mqtt_client.py

# 3. 测试远程开锁
python3 remote_comm.py --test
```

### 6.2 云端测试

```bash
# 1. 测试 API 接口
curl -X POST http://8.134.196.56:5000/api/unlock \
  -H "Content-Type: application/json" \
  -d '{"user_id": "test", "method": "curl"}'

# 2. 检查设备状态
curl http://8.134.196.56:5000/api/status?device_id=raspi_doorbell_001

# 3. 健康检查
curl http://8.134.196.56:5000/api/health
```

### 6.3 MQTT 测试

使用在线 MQTT 客户端测试：https://www.emqx.com/zh/mqtt-public-broker

```
订阅主题：doorbell/status
发布主题：doorbell/unlock
发布消息：{"action": "unlock", "user_id": "test"}
```

---

## 七、部署到服务器

### 7.1 一键部署

```bash
# 登录服务器
ssh root@8.134.196.56

# 执行部署脚本
cd /path/to/graduation
bash deploy/deploy.sh
```

### 7.2 手动部署

```bash
# 1. 安装依赖
sudo yum install -y python3 python3-pip git
pip3 install flask flask-cors paho-mqtt requests

# 2. 创建服务目录
sudo mkdir -p /opt/doorbell
cd /opt/doorbell

# 3. 启动服务
python3 server/routes/device_control.py
```

### 7.3 配置 systemd 服务

```bash
# 创建服务文件
sudo tee /etc/systemd/system/doorbell-server.service > /dev/null << 'EOF'
[Unit]
Description=CLAUDE 智能门铃系统 - 云服务器 API
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/opt/doorbell
ExecStart=/usr/bin/python3 /opt/doorbell/server.py
Restart=always

[Install]
WantedBy=multi-user.target
EOF

# 启动服务
sudo systemctl daemon-reload
sudo systemctl enable doorbell-server
sudo systemctl start doorbell-server
```

---

## 八、故障排查

### 8.1 MQTT 连接失败

```bash
# 检查网络连接
ping broker.emqx.io

# 检查端口连通性
telnet broker.emqx.io 1883

# 检查 Python 库
pip list | grep paho-mqtt
```

### 8.2 API 接口无法访问

```bash
# 检查服务器状态
ssh root@8.134.196.56

# 检查服务运行
sudo systemctl status doorbell-server

# 查看日志
sudo journalctl -u doorbell-server -f

# 检查防火墙
sudo firewall-cmd --list-ports
```

### 8.3 步进电机不工作

```bash
# 检查 GPIO 权限
gpioinfo

# 检查接线
# IN1 -> GPIO 1
# IN2 -> GPIO 7
# IN3 -> GPIO 8
# IN4 -> GPIO 25

# 检查 lgpio 库
pip list | grep lgpio
```

---

## 九、安全建议

1. **API 认证**: 生产环境应添加 JWT Token 认证
2. **HTTPS**: 使用 Nginx 反向代理启用 HTTPS
3. **MQTT 认证**: 使用带认证的 MQTT Broker
4. **设备绑定**: 实现设备唯一标识绑定
5. **日志审计**: 记录所有开锁操作

---

## 十、文件清单

```
graduation/
├── config/
│   └── settings.json           # 系统配置
├── src/
│   └── sync/
│       ├── __init__.py         # 模块导出
│       ├── cloud_config.py     # 云服务器配置
│       ├── remote_unlock.py    # 远程开锁控制
│       ├── data_sync.py        # 数据同步
│       └── api_server.py       # 本地 API
├── server/
│   └── routes/
│       ├── device_control.py   # 设备控制 API
│       └── command.py          # 命令下发
├── deploy/
│   └── deploy.sh              # 部署脚本
├── remote_comm.py              # 远程通信主程序
└── docs/
    └── 服务器连接指南.md        # 本文档
```

---

## 十一、联系方式

如有问题，请联系开发团队或查阅项目文档。
