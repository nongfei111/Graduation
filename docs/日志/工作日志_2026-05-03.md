# 工作日志（2026-05-03）

## 今日目标

- 打通“App 上传成员照片 → 树莓派同步人脸库 → 识别成功开锁 → 访客记录写入云端并在 App 展示”的主闭环
- 修复 App 端错误提示与云端返回结构不一致导致的“功能全失败/开锁失败原因丢失”
- 梳理并精简项目冗余文件，补齐部署与运行脚本

## 已完成

### 1) 云端（8.134.196.56:5000）

- 更新并重启 `smart-doorbell.service`，健康检查通过 `/api/health`
- 修复统计接口 `/api/stats` 的游标取值方式导致的异常（减少 App 端“加载统计失败”类报错）
- 访客列表 `/api/visitor/list` 修复 `family_id` 解析错误（DictCursor 下不应使用 `result[0]`）
- 成员管理
  - `/api/device/members` 用于设备侧同步成员信息（返回 `members[].face_image` URL）
  - 成员删除 `/api/member/<id>/delete` 删除记录时补齐成员照片文件清理（`uploads/members/...`）
- 访客上报 `/api/visitor/upload`
  - 兼容 `visitor_type`：把 `member` 视为 `family`，把 `unknown_visitor/unknown` 视为 `stranger`，避免数据库 ENUM 写入失败

### 2) 树莓派（设备端）

- 显示屏实时画面可用（OpenCV 窗口正常显示）
- 定位并解决“欢迎页不切换到画面/SSH 无 DISPLAY 崩溃/日志目录不存在”等运行问题
  - 运行目录统一以 `/home/aaa/Desktop/graduation` 为准（避免 `data/faces`、`data/models` 相对路径错位）
  - `logs/` 不存在导致启动失败的问题：启动前创建目录；主程序已支持自动创建日志目录
- 新增固定启动脚本：`run_main.sh`
  - 自动创建 `logs/`
  - 自动加载同目录 `.env.pi`（导出 `SERVER_HOST/SERVER_PORT/DEVICE_TOKEN` 等）
  - 自动设置 `DISPLAY=:0`（在可用时）
  - 优先使用 `venv/bin/python`
- 修复“删除成员后仍能识别开锁”
  - 成员同步逻辑会清理本地已不在云端的 `cloud_member_*.jpg`，并强制重建 `faces_cache.pkl`
- 修复“访客上报静默失败/成功计数不准”
  - 统一 `CloudCommunication.upload_visitor` 定义（避免被重复定义覆盖）
  - `DataSync` 上报时将 `unknown_visitor` 映射为 `stranger`，并携带抓拍图上传
- 性能优化（缓解识别时画面卡顿）
  - 人脸检测在大分辨率下先缩放后检测，再映射回原图坐标
  - 特征提取前对人脸裁剪图做缩放上限

### 3) Android（App）

- 修复“开锁失败：开锁失败”与“原因丢失”
  - 适配云端 `/api/control/unlock` 扁平返回字段（`command_id/message/error`）
  - Repository 解析 `errorBody` 并透传原因，Toast 统一显示“开锁失败：原因”
- 访客类型展示兼容 `family/member`
- 增加 `gradle.properties`（启用 AndroidX/Jetifier）以提升构建稳定性

### 4) 项目整理

- 梳理项目结构（服务器 / 硬件 / App / 配置 / 数据库 / 文档）
- 删除未使用/旧分支/损坏文件与目录：
  - `app_server.py`、`face_module.py`、`face_display.py`
  - `android_app/`、`face_recognition_module/`、`src/modules/`、`src/ui/`
  - 清理构建产物与缓存：`.gradle/`、`app/build/`、`__pycache__/`

## 关键目录约定（本地/云端）

### 云端

- 成员上传照片：`/home/smart_doorbell/server/uploads/members/`
- 访客抓拍照片：`/home/smart_doorbell/server/uploads/visitors/`

### 树莓派（以运行目录为根）

- 成员人脸库：`data/faces/<成员名>/cloud_member_<id>.jpg`
- 识别缓存：`data/models/faces_cache.pkl`
- 陌生访客抓拍：`data/unknown_faces/unknown_*.jpg`

## 已知风险/注意事项

- `Config.FACE_DATA_DIR`、`Config.MODEL_DATA_DIR` 为相对路径，运行目录不同会导致人脸库与缓存错位
  - 统一从 `/home/aaa/Desktop/graduation` 启动（`bash run_main.sh`）
- 设备鉴权依赖 `DEVICE_TOKEN`
  - `.env.pi` 必须存在于运行目录且填写真实 token（日志中建议仅打印前后片段，避免泄露）
- 不要用 `^Z` 挂起主进程（会导致旧环境变量/旧连接仍保持）；用 `Ctrl+C` 正常退出再重启

## 下一步（待验证清单）

- 树莓派成功从云端同步到新增成员（如 nf/cs）的 `cloud_member_*.jpg`
- 识别成功后云端写入 `visitors`，App 访客列表可刷新展示抓拍图
- 删除成员后设备端同步清理本地人脸库并无法再识别开锁

