# 修复验证清单 - 2026-04-20

## 修复完成的三个问题

### ✅ 1. 删除成员 404 错误
**问题**: Android API URL 不匹配  
**修复**: 添加 `/delete` 后缀到 URL  
**状态**: 已验证通过

### ✅ 2. 照片上传不显示
**问题**: 
- Flask 未配置静态文件服务
- 服务器返回本地路径而非 HTTP URL

**修复**:
1. 添加 `/uploads/<path>` 路由服务静态文件
2. 成员列表 API 返回完整 HTTP URL
3. Android Adapter 从 URL 加载照片

**状态**: 服务器已重启，等待 APK 测试

### ✅ 3. 添加成员显示失败但实际成功
**问题**: Retrofit `@Part` 序列化 String 时添加 JSON 引号  
**修复**: 使用 `RequestBody` 发送纯文本  
**状态**: 代码已更新，等待 APK 测试

---

## 服务器状态

```
服务器: 8.134.196.56:5000
进程 ID: 2993
状态: 运行中
日志：/tmp/flask.log
```

### 数据库状态
```sql
mysql> SELECT id, family_id, name, face_image_path FROM members;
+----+-----------+------+----------------------------------------------+
| id | family_id | name | face_image_path                              |
+----+-----------+------+----------------------------------------------+
|  8 |         3 | 妈妈  | uploads/members/member_3_8_20260420181228.jpg |
+----+-----------+------+----------------------------------------------+
```

---

## APK 信息

**路径**: `app/build/outputs/apk/debug/app-debug.apk`  
**大小**: 8.5MB  
**编译时间**: 2026-04-20 18:24  
**状态**: ✅ 编译成功

---

## 测试步骤

1. **卸载旧版本**
   ```bash
   adb uninstall com.example.smartdoorbell
   ```

2. **安装新 APK**
   ```bash
   adb install app/build/outputs/apk/debug/app-debug.apk
   ```

3. **测试清单**
   - [ ] 添加成员（应显示"添加成功"）
   - [ ] 上传照片（头像应立即显示）
   - [ ] 刷新列表（照片应正常显示）
   - [ ] 删除成员（应成功删除）

---

## 修改文件清单

### 服务器端
- `cloud_server/server/app.py`
  - 添加静态文件路由 `/uploads/<path>`
  - 成员列表返回完整 HTTP URL
  - 添加成员 API 返回照片 URL

### Android 端
- `ApiService.kt`: DELETE URL 添加 `/delete`，name 参数改为 RequestBody
- `Repositories.kt`: 创建 RequestBody 发送纯文本
- `MemberAdapter.kt`: 从服务器 URL 加载照片

---

## 重要提示

每次修改后必须：
1. **服务器代码** → 重启 `pkill -9 python && python app.py`
2. **Android 代码** → 重新编译安装 `./gradlew assembleDebug && adb install`

---

**记录时间**: 2026-04-20 18:25  
**状态**: 等待 APK 安装验证
