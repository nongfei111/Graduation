# 云服务器配置与使用连接指南

## 一、服务器信息

### 1.1 阿里云服务器配置

| 项目 | 配置值 |
|------|--------|
| **实例 ID** | 7a518455b93b47cca76ffbe076ae2112 |
| **实例名称** | Alibaba Cloud Linux-vmgc |
| **公网 IP** | 8.134.196.56 |
| **私有 IP** | 172.19.47.170 |
| **地域** | 华南 3（广州） |
| **镜像** | Alibaba Cloud Linux 3.21.04 |
| **运行状态** | 运行中 |
| **到期时间** | 2026 年 10 月 11 日 00:00:00 |

### 1.2 服务端口

| 端口 | 用途 | 状态 |
|------|------|------|
| 80 | HTTP (Nginx) | 需开放 |
| 5000 | Flask API | 需开放 |
| 1883 | MQTT Broker | 公共 Broker |

---

## 二、服务器部署步骤

### 2.1 SSH 登录服务器

```bash
# 使用 root 用户登录
ssh root@8.134.196.56

# 如果有密钥文件
ssh -i ~/.ssh/your_key root@8.134.196.56
```

### 2.2 一键部署

```bash
# 上传项目到服务器
scp -r C:\Users\HP\Desktop\graduation root@8.134.196.56:/root/

# 登录服务器执行部署脚本
ssh root@8.134.196.56
cd /root/graduation/deploy
bash deploy_cloud_server.sh
```

### 2.3 手动部署（可选）

如果不想使用自动脚本，可以手动执行：

```bash
# 1. 安装依赖
yum update -y
yum install -y python3 python3-pip git nginx

# 2. 创建应用目录
mkdir -p /opt/doorbell/{logs,data,uploads}
cd /opt/doorbell

# 3. 创建虚拟环境
python3 -m venv venv
source venv/bin/activate

# 4. 安装 Python 依赖
pip install flask flask-cors flask-jwt-extended
pip install paho-mqtt requests
pip install gunicorn

# 5. 创建应用文件（参考 deploy_cloud_server.sh 中的代码）
# ... 创建 server_config.py 和 doorbell_server.py

# 6. 启动服务
python doorbell_server.py
```

---

## 三、配置阿里云安全组

### 3.1 开放端口

登录 [阿里云控制台](https://ecs.console.aliyun.com/) → 防火墙/安全组 → 添加规则：

| 端口范围 | 授权对象 | 描述 |
|----------|----------|------|
| 80/80 | 0.0.0.0/0 | HTTP 访问 |
| 5000/5000 | 0.0.0.0/0 | API 服务 |

### 3.2 配置步骤

1. 登录阿里云控制台
2. 进入 ECS 实例详情页
3. 点击"安全组" → "配置规则"
4. 添加入方向规则：
   - 端口：80, 5000
   - 授权对象：0.0.0.0/0
   - 协议：TCP

---

## 四、API 接口使用

### 4.1 健康检查

```bash
# 请求
curl http://8.134.196.56/api/health

# 响应
{
    "status": "ok",
    "timestamp": "2026-04-10T12:30:00",
    "mqtt_connected": true,
    "server_ip": "8.134.196.56"
}
```

### 4.2 获取服务器信息

```bash
# 请求
curl http://8.134.196.56/api/info

# 响应
{
    "success": true,
    "data": {
        "instance_id": "7a518455b93b47cca76ffbe076ae2112",
        "instance_name": "Alibaba Cloud Linux-vmgc",
        "region": "华南 3（广州）",
        "public_ip": "8.134.196.56",
        "private_ip": "172.19.47.170",
        "mirror": "Alibaba Cloud Linux 3.21.04",
        "expire_date": "2026 年 10 月 11 日 00:00:00",
        "status": "运行中",
        "api_port": 5000,
        "mqtt_broker": "broker.emqx.io:1883"
    }
}
```

### 4.3 远程开锁

```bash
# 请求
curl -X POST http://8.134.196.56/api/unlock \
  -H "Content-Type: application/json" \
  -d '{
    "user_id": "user123",
    "device_id": "raspi_doorbell_001"
  }'

# 响应
{
    "success": true,
    "message": "开锁指令已发送",
    "timestamp": "2026-04-10T12:30:00"
}
```

### 4.4 远程上锁

```bash
# 请求
curl -X POST http://8.134.196.56/api/lock \
  -H "Content-Type: application/json" \
  -d '{"user_id": "user123"}'

# 响应
{
    "success": true,
    "message": "上锁指令已下发"
}
```

### 4.5 获取设备状态

```bash
# 请求
curl "http://8.134.196.56/api/status?device_id=raspi_doorbell_001"

# 响应
{
    "success": true,
    "data": {
        "device_id": "raspi_doorbell_001",
        "status": "online",
        "last_seen": "2026-04-10T12:30:00",
        "mqtt_broker": "broker.emqx.io",
        "mqtt_connected": true
    }
}
```

### 4.6 获取访客记录

```bash
# 请求
curl http://8.134.196.56/api/visitors

# 响应
{
    "success": true,
    "count": 5,
    "visitors": [...]
}
```

### 4.7 获取访问日志

```bash
# 请求
curl http://8.134.196.56/api/logs

# 响应
{
    "success": true,
    "count": 10,
    "logs": [...]
}
```

---

## 五、树莓派端配置

### 5.1 配置文件

编辑树莓派上的 `config/settings.json`:

```json
{
  "system": {
    "device_id": "raspi_doorbell_001"
  },
  "network": {
    "server_host": "8.134.196.56",
    "server_port": 5000,
    "mqtt_broker": "broker.emqx.io",
    "mqtt_port": 1883
  },
  "motor": {
    "type": "28BYJ-48",
    "pins": [1, 7, 8, 25],
    "steps_per_rev": 2048,
    "unlock_angle": 90
  }
}
```

### 5.2 启动远程通信服务

```bash
# 安装依赖
pip install paho-mqtt requests

# 测试模式（模拟硬件）
python remote_comm.py --simulator

# 正常模式（真实硬件）
python remote_comm.py
```

### 5.3 测试连接

```bash
# 在树莓派上测试服务器连接
curl http://8.134.196.56/api/health

# 测试 MQTT 连接
python -c "
import paho.mqtt.client as mqtt
client = mqtt.Client()
client.connect('broker.emqx.io', 1883, 60)
client.publish('doorbell/status', '{\"device_id\":\"test\",\"status\":\"online\"}')
print('MQTT 消息已发送')
"
```

---

## 六、手机 APP 集成

### 6.1 API 调用示例（Android/Kotlin）

```kotlin
// 远程开锁
suspend fun remoteUnlock(userId: String): Boolean {
    val client = OkHttpClient()
    val json = JSONObject()
    json.put("user_id", userId)
    
    val body = RequestBody.create(
        MediaType.parse("application/json"),
        json.toString()
    )
    
    val request = Request.Builder()
        .url("http://8.134.196.56/api/unlock")
        .post(body)
        .build()
    
    val response = client.newCall(request).execute()
    return response.isSuccessful
}
```

### 6.2 API 调用示例（iOS/Swift）

```swift
// 远程开锁
func remoteUnlock(userId: String, completion: @escaping (Bool) -> Void) {
    let url = URL(string: "http://8.134.196.56/api/unlock")!
    var request = URLRequest(url: url)
    request.httpMethod = "POST"
    request.setValue("application/json", forHTTPHeaderField: "Content-Type")
    
    let body: [String: Any] = ["user_id": userId]
    request.httpBody = try? JSONSerialization.data(withJSONObject: body)
    
    URLSession.shared.dataTask(with: request) { data, response, error in
        let success = (response as? HTTPURLResponse)?.statusCode == 200
        completion(success)
    }.resume()
}
```

---

## 七、MQTT 通信测试

### 7.1 使用在线 MQTT 客户端

访问 [EMQX Public MQTT Broker](https://www.emqx.com/zh/mqtt/public-mqtt5-broker)

**订阅主题测试**:
1. 订阅 `doorbell/status`
2. 查看树莓派发布的设备状态

**发布开锁指令**:
1. 发布到主题 `doorbell/unlock`
2. 消息内容：`{"action":"unlock","user_id":"test","timestamp":"2026-04-10T12:00:00"}`

### 7.2 使用 Python 测试

```python
import paho.mqtt.client as mqtt
import json
import time

# 回调函数
def on_connect(client, userdata, flags, rc):
    print(f"已连接，返回码：{rc}")
    client.subscribe("doorbell/status")

def on_message(client, userdata, msg):
    print(f"收到消息：{msg.topic} - {msg.payload.decode()}")

# 创建客户端
client = mqtt.Client(client_id="test_client")
client.on_connect = on_connect
client.on_message = on_message

# 连接
client.connect("broker.emqx.io", 1883, 60)

# 发布开锁指令
unlock_msg = {
    "action": "unlock",
    "user_id": "test_user",
    "timestamp": time.time()
}
client.publish("doorbell/unlock", json.dumps(unlock_msg))
print("开锁指令已发送")

# 循环接收消息
client.loop_forever()
```

---

## 八、服务器管理

### 8.1 查看日志

```bash
# 查看服务日志
journalctl -u doorbell-server -f

# 查看应用日志
cat /var/log/doorbell/server.log

# 查看 Nginx 日志
tail -f /var/log/nginx/access.log
tail -f /var/log/nginx/error.log
```

### 8.2 服务管理

```bash
# 启动服务
systemctl start doorbell-server

# 停止服务
systemctl stop doorbell-server

# 重启服务
systemctl restart doorbell-server

# 查看状态
systemctl status doorbell-server

# 开机自启
systemctl enable doorbell-server
```

### 8.3 数据库管理

```bash
# 进入 SQLite 数据库
sqlite3 /opt/doorbell/data/doorbell.db

# 查看表
.tables

# 查看用户
SELECT * FROM users;

# 查看开锁记录
SELECT * FROM access_logs ORDER BY created_at DESC LIMIT 10;
```

---

## 九、故障排查

### 9.1 服务器无法访问

```bash
# 检查服务器状态
ssh root@8.134.196.56

# 检查服务运行
systemctl status doorbell-server
systemctl status nginx

# 检查端口监听
netstat -tlnp | grep 5000
netstat -tlnp | grep 80

# 检查防火墙
firewall-cmd --list-ports
```

### 9.2 MQTT 连接失败

```bash
# 检查网络
ping broker.emqx.io

# 测试端口
telnet broker.emqx.io 1883

# 检查 Python 库
pip list | grep paho-mqtt
```

### 9.3 API 返回错误

```bash
# 查看应用日志
tail -f /var/log/doorbell/server.log

# 重启服务
systemctl restart doorbell-server

# 测试健康检查
curl http://localhost:5000/api/health
```

---

## 十、安全建议

### 10.1 启用 HTTPS

```bash
# 使用 Let's Encrypt 获取证书
yum install certbot python3-certbot-nginx
certbot --nginx -d your-domain.com
```

### 10.2 添加 API 认证

在 `doorbell_server.py` 中添加 JWT 认证：

```python
from flask_jwt_extended import JWTManager, create_access_token, jwt_required

app.config['JWT_SECRET_KEY'] = 'your-secret-key'
jwt = JWTManager(app)

@app.route('/api/auth/login', methods=['POST'])
def login():
    data = request.get_json()
    # 验证用户
    access_token = create_access_token(identity=data['username'])
    return jsonify(access_token=access_token)

@app.route('/api/unlock', methods=['POST'])
@jwt_required()
def remote_unlock():
    # 需要 JWT token 才能调用
    ...
```

### 10.3 配置防火墙

```bash
# 仅允许特定 IP 访问
firewall-cmd --permanent --add-rich-rule='
  rule family="ipv4" source address="YOUR_HOME_IP" 
  port port="5000" protocol="tcp" accept'
firewall-cmd --reload
```

---

## 十一、快速参考

### 服务器地址汇总

| 服务 | 地址 |
|------|------|
| API (HTTP) | http://8.134.196.56:5000 |
| API (Nginx) | http://8.134.196.56 |
| MQTT Broker | broker.emqx.io:1883 |

### 常用命令

```bash
# 测试 API
curl http://8.134.196.56/api/health
curl http://8.134.196.56/api/info
curl -X POST http://8.134.196.56/api/unlock -H "Content-Type: application/json" -d '{"user_id":"test"}'

# 服务器管理
ssh root@8.134.196.56
systemctl status doorbell-server
journalctl -u doorbell-server -f
```

---

## 十二、联系支持

如遇到问题，请检查：
1. 服务器日志：`/var/log/doorbell/server.log`
2. 系统日志：`journalctl -u doorbell-server`
3. Nginx 日志：`/var/log/nginx/`
