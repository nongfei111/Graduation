# 工作日志 - 2026-04-20 (服务器重启)

## 一、今日完成工作

### 1.1 问题修复总结

| 问题 | 状态 | 说明 |
|------|------|------|
| 删除成员 404 | ✅ 已解决 | URL 路径不匹配，已添加 `/delete` 后缀 |
| 添加成员显示失败 | ⏳ 待验证 | 使用 RequestBody 发送纯文本名字 |
| 照片上传不显示 | ⏳ 待验证 | 服务器添加静态文件路由 |

### 1.2 服务器端修改

**文件**: `cloud_server/server/app.py`

1. **添加静态文件路由**
```python
@app.route('/uploads/<path:filename>')
def serve_upload(filename):
    """提供上传文件的访问"""
    return send_from_directory(UPLOAD_DIR, filename)
```

2. **成员列表 API 返回完整 URL**
```python
face_image_url = m['face_image_path']
if face_image_url:
    face_image_url = "http://8.134.196.56:5000/" + face_image_url.replace("/home/smart_doorbell/server/", "")
```

3. **添加成员 API 返回照片 URL**
```python
face_image_url = None
if photo_path:
    face_image_url = "http://8.134.196.56:5000/" + photo_path.replace("/home/smart_doorbell/server/", "")
```

### 1.3 Android 端修改

**文件**: `ApiService.kt`, `Repositories.kt`, `MemberAdapter.kt`

1. **DELETE URL 修复**
```kotlin
@DELETE("/api/member/{memberId}/delete")  // 添加 /delete 后缀
```

2. **添加成员使用 RequestBody**
```kotlin
@Part("name") name: okhttp3.RequestBody  // 而非 String

// Repository 中创建
val nameBody = name.toRequestBody("text/plain".toMediaType())
```

3. **从 URL 加载照片**
```kotlin
val imageUrl = if (photoUrl.startsWith("http")) {
    photoUrl
} else {
    "http://8.134.196.56:5000${photoUrl.replace("/home/smart_doorbell/server", "")}"
}
Glide.with(context).load(imageUrl)...
```

### 1.4 服务器重启

```bash
# 停止旧进程
kill -9 2825

# 启动新进程
cd /home/smart_doorbell/server
nohup /home/smart_doorbell/venv/bin/python app.py > /tmp/flask.log 2>&1 &
```

**健康检查结果**:
```json
{
  "server": "CLAUDE Smart Doorbell Cloud",
  "status": "ok",
  "timestamp": "2026-04-20T18:22:51.389645"
}
```

### 1.5 APK 编译

**命令**: `./gradlew assembleDebug --no-daemon`  
**结果**: BUILD SUCCESSFUL  
**APK 路径**: `app/build/outputs/apk/debug/app-debug.apk`  
**大小**: 8.5MB  
**时间**: 2026-04-20 18:24

---

## 二、待解决问题（明日）

### 2.1 问题 1: 照片上传成功但不显示

**现象**:
- 上传提示"照片已上传"
- 数据库已保存路径
- 但头像显示默认图标

**可能原因**:
1. Android Adapter 未正确加载 URL
2. 服务器静态文件路由未生效
3. Glide 加载失败（权限/网络问题）

**调试步骤**:
```bash
# 1. 检查服务器日志
ssh root@8.134.196.56 "tail -50 /tmp/flask.log"

# 2. 测试静态文件访问
curl http://8.134.196.56:5000/uploads/members/xxx.jpg

# 3. 检查 Android Logcat
adb logcat | grep -i glide
```

**修复验证**:
- [ ] 服务器 `/uploads/` 路由可访问
- [ ] 成员列表 API 返回完整 HTTP URL
- [ ] Android 从 URL 加载而非本地文件

### 2.2 问题 2: 添加成员显示失败但实际成功

**现象**:
- 点击添加后显示失败
- Toast 显示 `java.lang.IllegalStateException`
- 刷新列表后成员已存在

**可能原因**:
1. 服务器响应格式解析问题
2. Retrofit 响应超时
3. 返回数据字段不匹配

**调试步骤**:
```kotlin
// 在 Repository 中打印原始响应
val response = apiService.addMember(...)
println("Response code: ${response.code()}")
println("Response body: ${response.raw().body()?.string()}")
```

**修复验证**:
- [ ] 服务器返回 JSON 格式正确
- [ ] Android 正确解析响应
- [ ] Toast 显示"添加成功"

---

## 三、数据库状态

### 3.1 成员表
```sql
mysql> SELECT id, family_id, name, face_image_path, is_active FROM members WHERE family_id = 3;
+----+-----------+----------------+----------------------------------------------+-----------+
| id | family_id | name           | face_image_path                              | is_active |
+----+-----------+----------------+----------------------------------------------+-----------+
|  8 |         3 | 妈妈           | uploads/members/member_3_8_20260420181228.jpg |         1 |
|  9 |         3 | 姐姐           | NULL                                         |         1 |
| 10 |         3 | qqq            | NULL                                         |         1 |
| 11 |         3 | www            | NULL                                         |         1 |
| 12 |         3 | 爸爸           | NULL                                         |         1 |
+----+-----------+----------------+----------------------------------------------+-----------+
```

### 3.2 照片文件
```bash
# 服务器照片目录
ls -la /home/smart_doorbell/server/uploads/members/

# 确认文件存在
member_3_8_20260420181228.jpg  # 妈妈 的照片
```

---

## 四、服务器配置

### 4.1 运行状态
```
进程 ID: 2993
端口：5000
日志：/tmp/flask.log
虚拟环境：/home/smart_doorbell/venv
```

### 4.2 网络监听
```bash
netstat -tlnp | grep 5000
tcp  0.0.0.0:5000  LISTEN
```

---

## 五、明日测试计划

### 5.1 安装新 APK
```bash
adb uninstall com.example.smartdoorbell
adb install app/build/outputs/apk/debug/app-debug.apk
```

### 5.2 测试清单
- [ ] 登录功能
- [ ] 添加成员（验证 Toast 提示）
- [ ] 上传成员照片（验证头像显示）
- [ ] 刷新成员列表（验证照片加载）
- [ ] 删除成员（验证成功）

### 5.3 日志收集
```bash
# Android Logcat
adb logcat -v time > logcat_$(date +%Y%m%d_%H%M%S).txt

# 服务器日志
ssh root@8.134.196.56 "tail -100 /tmp/flask.log"
```

---

## 六、重要提示

### 6.1 修改后必须执行
1. **服务器代码修改** → `pkill -9 python && python app.py`
2. **Android 代码修改** → `./gradlew assembleDebug && adb install`

### 6.2 排查问题顺序
1. 先看服务器日志 `/tmp/flask.log`
2. 再看 Android Logcat
3. 检查数据库状态
4. 验证文件是否存在

---

**记录者**: Claude Code  
**记录时间**: 2026-04-20 18:30  
**状态**: 服务器已重启，APK 已编译，等待明日测试
